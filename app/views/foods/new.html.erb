<div class="container">
<h1>Nueva comida</h1>
<p>Estas son las comidas que los clientes ven y seleccionan</p>
<%= link_to 'Regresar', foods_path, class: 'btn btn-light btn-small' %>
<br>
<br>
<% if @food.errors.any? %>
    <div class="alert alert-danger" role="alert">
    <div id="error_explanation">
      <h4><%= pluralize(@food.errors.count, "error")%></h4>
      <ul>
      <% @food.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
    </div>
  <% end %>
<%= form_for @food do |f| %>
<div class="form-group">
  <%= f.label 'Categoría' %><br />
  <%= f.select :food_category_id, options_from_collection_for_select(@categories, 'id', 'name'), {}, { :class => 'form-control form-control-lg' } %>
</div>
<div class="form-group">
  <%= f.label 'Nombre' %><br />
  <%= f.text_field :name, autofocus: true, autocomplete: "content", placeholder: "Escribe el nombre de la comida", :class => 'form-control form-control-lg' %>
</div>
  <%= f.submit 'Guardar', :class => 'btn btn-primary' %>
<% end %>
</div>

