<div class="container">
<h1>Agregar comidas </h1>
<%= link_to 'Regresar', edit_diet_path(:id => @meal.diet_id) , class: 'btn btn-light btn-small' %>
<br><br>
<p>Agregar las comidas en el orden que se deben realizar. El nombre sirve para que los clientes identifiquen si se trata del desayuno, colación, cena, etc.</p>
<% if @meal.errors.any? %>
  <div class="alert alert-danger" role="alert">
  <div id="error_explanation">
    <h4><%= pluralize(@meal.errors.count, "error")%></h4>
    <ul>
    <% @meal.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  </div>
<% end %>
<%= form_for @meal do |f| %>
  <div class="form-group">
  <%= f.label 'Día de la semana' %><br /> 
  <%= f.select :weekday, options_for_select([['Lunes', :monday], ['Martes', :tuesday], ['Miércoles', :wednesday], ['Jueves', :thursday], ['Viernes', :friday], ['Sábado', :saturday], ['Domingo', :sunday]] , params[:weekday]), {}, { :class => 'form-control form-control-lg' } %>

  </div>
      <div class="form-group">
    <%= f.label :nombre %><br />
    <%= f.text_field :name, autofocus: true, list: "names", :class => 'form-control form-control-lg' %>
    <datalist id="names">
      <option value="Desayuno">
      <option value="Colación de medio día">
      <option value="Almuerzo">
      <option value="Colación de la tarde">
      <option value="Cena">
    </datalist>
</div>
<%= f.hidden_field :diet_id, value: @meal.diet_id %>
<div class="form-group">
    <%= f.label :contenido %><br />
    <%= f.text_area :content, autofocus: true, autocomplete: "content", :class => 'form-control form-control-lg' %>
</div>
    <%= f.submit 'Guardar', :class => 'btn btn-primary' %>
  <% end %>

</div>

